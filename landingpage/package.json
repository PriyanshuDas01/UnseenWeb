{
  "name": "purescript-landing-page",
  "version": "1.0.0",
  "description": "A minimal landing page built with PureScript and Halogen",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && spago build && esbuild index.js --bundle --outfile=public/bundle.js --format=esm && cp index.html style.css hero-ai.png public/",
    "start": "serve public",
    "clean": "rm -rf output dist public"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "purescript": "^0.15.0",
    "spago": "^0.20.0",
    "serve": "^14.0.0"
  }
} 